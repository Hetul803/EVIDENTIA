// Fallback text when no transcript is available.
export function getAudioPlaceholderText(): string {
  return "[Audio file provided. Full transcription not available in this build. Analysis will use file metadata and any available transcript.]";
}
