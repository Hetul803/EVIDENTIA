# Evidentia - Truth Engine
# Copy to .env and fill in your keys

# Database (optional)
# Privacy-first mode does not store reports in a DB.
# DATABASE_URL="file:./dev.db"

# Gemini API (Google AI Studio https://aistudio.google.com/apikey)
GEMINI_API_KEY=""
# Optional: override model (default: gemini-3-flash-preview). See https://ai.google.dev/gemini-api/docs/models
# GEMINI_MODEL="gemini-3-flash-preview"

# External verification (choose one) — RECOMMENDED FREE: Tavily
# Tavily (best free tier for AI/search): https://tavily.com/ — sign up, get API key, 1000 free searches/month
# SerpAPI: https://serpapi.com/ — 100 free searches/month
# Leave empty for "no external verification" (report still works, citations unavailable)
SEARCH_API_PROVIDER="tavily"
# For Tavily: set TAVILY_API_KEY (or SEARCH_API_KEY). For SerpAPI: set SEARCH_API_KEY.
TAVILY_API_KEY=""
# SEARCH_API_KEY=""   # use for Tavily or SerpAPI if you prefer one key
# Options: serpapi | tavily | none
